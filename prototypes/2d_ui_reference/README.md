# Reference-only 2D prototype

This folder preserves the rejected flat desktop presentation for visual and interaction reference only.

It is not the authoritative VectorVerse product scene. The authoritative runtime is `res://Main.tscn`, a spatial `Node3D` world.
